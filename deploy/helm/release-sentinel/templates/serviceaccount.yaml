{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "release-sentinel.serviceAccountName" . }}
  labels:
    {{- include "release-sentinel.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
