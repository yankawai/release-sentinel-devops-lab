apiVersion: v1
kind: Service
metadata:
  name: {{ include "release-sentinel.fullname" . }}
  labels:
    {{- include "release-sentinel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "release-sentinel.selectorLabels" . | nindent 4 }}
